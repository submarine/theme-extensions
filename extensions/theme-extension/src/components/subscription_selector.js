import SubmarineSelector from "./submarine_selector";

/**
 * subscription_selector.js
 *
 * The subscription selector web component, which extends Submarine's base selector web component
 * and should be added to a product form. It parses and displays information about the subscription
 * plans available for the currently selected variant, and allows adding it to cart.
 */
export default class SubscriptionSelector extends SubmarineSelector {}
