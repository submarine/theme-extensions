import SubmarineSelector from "./submarine_selector";

/**
 * crowdfunding_selector.js
 *
 * The crowdfunding selector web component, which extends Submarine's base selector web component
 * and should be added to a product form. It parses and displays information about the crowdfunding
 * options available for the currently selected variant, and allows adding it to cart.
 */
export default class CrowdfundingSelector extends SubmarineSelector {}
