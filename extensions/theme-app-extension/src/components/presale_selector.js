import SubmarineSelector from "./submarine_selector";

/**
 * presale_selector.js
 *
 * The presale selector web component, which extends Submarine's base selector web component
 * and should be added to a product form. It parses and displays information about the presale
 * options available for the currently selected variant, and allows adding it to cart.
 */
export default class PresaleSelector extends SubmarineSelector {}
